This is a placeholder.

Rebuild the app with the updated project_tracker.spec (see the README one
folder up for why — the spec was just updated to include the new Assets,
Products, and Sequences features, and the old dist/ build predates that).

From the project root, with trackervenv active:

    pyinstaller project_tracker.spec

Then copy everything from dist/ProjectTracker/ into this folder, replacing
this file.
